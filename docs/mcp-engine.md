# MCP Engine

> **Removed.** The Model Context Protocol (MCP) integration has been removed from Jandaira.
>
> All MCP-related code (`internal/mcp/`, `internal/model/mcp_server.go`, `internal/repository/mcp_server.go`, `internal/service/mcp_server.go`, `internal/api/mcp_handler.go`, `internal/security/command.go`) and the `/api/colmeias/:id/mcp-servers` REST endpoints no longer exist.
